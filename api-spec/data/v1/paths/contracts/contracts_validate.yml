post:
  tags:
    - Contracts
  summary: Validate schema change (compatibility, breaking)
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/ContractValidateRequest.yml"
  responses:
    "200":
      description: Validation result
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/ContractValidateResult.yml"
