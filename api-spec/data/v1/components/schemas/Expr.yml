type: object
description: "EN: Column reference or function call / TH: การอ้างอิงคอลัมน์หรือการเรียก function"
properties:
  col:
    type: string
    description: "EN: alias.column / TH: รูปแบบ alias.column"
  func:
    type: string
    enum:
      - max
      - min
      - sum
      - avg
      - count
      - coalesce
      - upper
      - lower
      - date_trunc
  args:
    type: array
    items:
      $ref: "./Expr.yml"
