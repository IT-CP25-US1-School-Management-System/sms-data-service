type: object
properties:
  status:
    type: string
    enum: [pending, in_progress, completed, failed]
  created_at:
    type: string
    format: date-time
  completed_at:
    type: string
    format: date-time
  started_at:
    type: string
    format: date-time
  destination_uri:
    type: string
  error_message:
    type: string