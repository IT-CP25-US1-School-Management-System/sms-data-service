type: object
properties:
  topic:
    type: string
  from:
    type: string
    format: date-time
  to:
    type: string
    format: date-time
  partitions:
    type: array
    items:
      type: integer
